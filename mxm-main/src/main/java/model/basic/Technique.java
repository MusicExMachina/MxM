package model.basic;

/**
 * A class representing an instrumental  technique, such as pizzicato, stopping, muting,
 * and so forth. Note that these  are techniques which alter what sound gets played.
 */
public class Technique {
    /** The default Technique. */
    public static final Technique DEFAULT = new Technique();
}
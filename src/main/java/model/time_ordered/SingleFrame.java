package model.time_ordered;

/**
 * A Frame which houses only a single MusicEvent.
 * This is essentially a "wrapped" MusicEvent,
 * giving it time-ordered context. Note that this is
 * still unrealized time- that is, it does not know
 * its literal playing timepoint.
 */
public class SingleFrame {
}
